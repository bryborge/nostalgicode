<!DOCTYPE html>
<html>
    <head>
        <title>Style-On Salon | Stylist</title>
        <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css'>
        <link rel='stylesheet' href='../css/styles.css'>
    </head>
    <body>
        <div class='container'>
            <div class='page-header'>
                <a href='/'><img src='../img/home.png'><h1>Style-On Salon</h1>
                <h5>unleash your inner chic</h5></a>
            </div>

            <div class='col-md-8'>
                <h3>Enter a new stylist</h3>

                <form action='/stylists' method='post'>
                    <label for='name'>Stylist</label>
                    <input name='name' type='text' class='form-control' id='name' required>

                    <button name='submit' class='btn btn-primary'>Add Stylist</button>
                </form>

            </div>

            <div class='col-md-4'>

                {% if stylists is not empty %}
                <h4>Here are your stylists:</h4>
                <ul>
                    {% for stylist in stylists %}
                        <li><a href='/stylists/{{ stylist.getId }}'>{{ stylist.getName }}</a></li>
                    {% endfor %}
                </ul>

                <form action='/' method='post'>
                    <input type='hidden' name='_method' value='delete'>

                    <button name='submit' class='btn btn-warning'>Delete All</button>
                </form>

                {% endif %}

            </div>

        </div><!-- CLOSING DIV CONTAINER -->

        <div class='page-footer' id='home-bottom'>
            <p><a href='/'>Home</a></p>
        </div>

    </body>
</html>
